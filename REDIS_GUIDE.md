# Инструкция по работе с Redis в боте Antigravity

В боте реализовано дублирование внутреннего состояния (`PositionState` и аналитики) в базу данных Redis. Это позволяет сторонним сервисам (веб-дашбордам, другим ботам, системам мониторинга) в реальном времени получать актуальные данные без обращения к файловой системе бота.

## Установка и запуск Redis

### Локально (на Windows для тестирования)

Самый простой способ поднять Redis локально — использовать Docker.

1. Установите **Docker Desktop**.
2. В терминале выполните команду:
   ```bash
   docker run --name my-redis -p 6379:6379 -d redis
   ```
3. Redis будет доступен по адресу `redis://localhost:6379/0`.

Если вы не хотите использовать Docker, вы можете установить порт Redis для Windows (например, из репозитория Memurai или устаревшие сборки от Microsoft), но Docker предпочтительнее.

### На сервере (Ubuntu / Debian)

На Linux Redis устанавливается нативно как системная служба:

1. Обновите пакеты и установите Redis:
   ```bash
   sudo apt update
   sudo apt install redis-server -y
   ```
2. Убедитесь, что служба запущена и работает:
   ```bash
   sudo systemctl status redis-server
   sudo systemctl enable redis-server
   ```
3. По умолчанию Redis слушает `localhost:6379`. Если ваш бот работает на этом же сервере, дополнительных настроек сети не требуется.

---

## Настройка бота (Включение Redis)

Чтобы бот начал отправлять данные в Redis, добавьте следующие строки в ваш файл `.env` в корне проекта:

```env
REDIS_ENABLED=true
REDIS_URL=redis://localhost:6379/0
```

> **Важно:** Бот использует библиотеку `redis.asyncio` (пакет `redis` в Python), который сам управляет асинхронными пулами соединений. Бот использует независимый механизм "debounce" (по умолчанию 1 секунда), что означает, что он будет группировать и отправлять пачки изменений в Redis не чаще, чем раз в секунду. Это полностью разгружает диск бота и не замедляет торговые циклы.

---

## Как устроены данные в Redis

Бот сохраняет данные, используя структуру `Hash` (Хэш-таблицы). Это идеальный формат для хранения JSON-документов по ключам.

1. **Ключ `bot:runtime:states`**
   - **Поля (Fields):** Имена символов, например `WIFUSDT`, `BTCUSDT`.
   - **Значения (Values):** Строка в формате JSON (полный дамп файла `runtime/<symbol>.json`).

2. **Ключ `bot:runtime:analytics`**
   - **Поля (Fields):** `global`
   - **Значение (Value):** Строка в формате JSON (полный дамп файла `analytics.json`).

---

## Как читать данные

### Через `redis-cli` (Командная строка)

Подключитесь к Redis:
```bash
redis-cli
```

Посмотреть список всех монет (полей), по которым есть стейты:
```bash
HKEYS bot:runtime:states
```

Получить JSON-стейт конкретной монеты (например, WIFUSDT):
```bash
HGET bot:runtime:states WIFUSDT
```

Получить данные глобальной аналитики:
```bash
HGET bot:runtime:analytics global
```

### Через Python скрипт (Рекомендуется для дашбордов)

В корневой папке проекта есть файл `test_redis_read.py`. Вы можете запустить его, чтобы наглядно убедиться, что данные успешно читаются.

Пример кода для внешнего сервиса (FastAPI / любого скрипта):

```python
import asyncio
import json
import redis.asyncio as redis

async def read_from_redis():
    # Подключаемся к базе
    client = redis.from_url("redis://localhost:6379/0", decode_responses=True)
    
    # Читаем стейт монеты WIFUSDT
    wif_state_str = await client.hget("bot:runtime:states", "WIFUSDT")
    if wif_state_str:
        state_data = json.loads(wif_state_str)
        long_state = state_data.get("LONG", {})
        print(f"WIFUSDT LONG In Position: {long_state.get('in_position')}")
        print(f"Avg Entry Price: {long_state.get('avg_entry_price')}")
        print(f"Total Volume: {long_state.get('total_volume')}")

    # Читаем аналитику
    analytics_str = await client.hget("bot:runtime:analytics", "global")
    if analytics_str:
        analytics_data = json.loads(analytics_str)
        print(f"\nGlobal Net Profit: {analytics_data.get('net_profit_usdt')} USDT")

    await client.close()

if __name__ == "__main__":
    asyncio.run(read_from_redis())
```








Я прописал новый адрес в `app.json`!

Отвечаю на ваши вопросы:

### 1. Как разворачивать Redis? Нужен ли ему Virtual Environment?
Нет, виртуальное окружение (`venv`) нужно **только для Python-библиотек**. Redis — это полноценная системная база данных (как PostgreSQL или MySQL), она написана на C и работает на уровне операционной системы.

Самый лучший и стабильный способ развернуть Redis на вашем сервере-арбитре — установить его глобально как системный сервис или через Docker:

**Вариант А (Простой, как системный сервис на Ubuntu/Debian):**
```bash
sudo apt update
sudo apt install redis-server
sudo systemctl enable redis-server
sudo systemctl start redis-server
```
*(Не забудьте в файле `/etc/redis/redis.conf` найти строку `bind 127.0.0.1` и изменить её на `bind 0.0.0.1` или закомментировать, чтобы Redis принимал подключения извне, а не только с самого сервера).*

**Вариант Б (Современный, через Docker):**
```bash
docker run -d --name redis-arbiter -p 6379:6379 --restart unless-stopped redis
```

### 2. Как выглядит внешний URL для Redis
Я уже обновил ваш `app.json`. Теперь адрес выглядит так:
`"url": "redis://103.27.132.132:6379/0"`

**⚠️ КРИТИЧЕСКИ ВАЖНЫЙ МОМЕНТ (БЕЗОПАСНОСТЬ):**
Если вы открываете порт Redis (6379) в интернет, чтобы к нему могли подключаться ваши ноды, его **обязательно нужно запаролить**. В интернете постоянно работают боты-сканеры. Открытый Redis без пароля взламывают за 10-15 минут, стирают все данные и устанавливают туда скрытые майнеры криптовалюты, которые сожрут процессор сервера.

Когда вы зададите пароль в настройках самого Redis (директива `requirepass ваш_супер_пароль` в `redis.conf`), в конфигах ботов URL нужно будет прописать вот в таком формате (я оставил подсказку в `_comment` в `app.json`):

`"url": "redis://:ваш_супер_пароль@103.27.132.132:6379/0"`






Я всё подготовил.

**Что было сделано:**
1. В `app.json` URL полностью убран, остался только флаг `"enabled": true` и подсказка.
2. Файл `consts.py` обновлен так, чтобы забирать переменную `REDIS_URL` из переменных окружения.
3. В файле `.env` я добавил строчку `REDIS_URL="redis://:ВАШ_ПАРОЛЬ_ОТ_REDIS@103.27.132.132:6379/0"`. Не забудьте вписать туда настоящий пароль! (Кстати, на второй ноде нужно будет в её `.env` прописать этот же `REDIS_URL`).

---

### Гайд: Как установить пароль и открыть Redis в сеть (Ubuntu/Debian)

Если вы ставили Redis через `apt install redis-server`, сделайте следующее прямо в консоли вашего сервера-арбитра (103.27.132.132):

**1. Откройте конфигурационный файл Redis:**
```bash
sudo nano /etc/redis/redis.conf
```

**2. Разрешите внешние подключения (по умолчанию он слушает только себя):**
Внутри файла найдите строчку (используйте `Ctrl+W` для поиска):
`bind 127.0.0.1 -::1`
Замените её на:
`bind 0.0.0.0`
*(Это заставит Redis принимать соединения с любого внешнего IP).*

**3. Установите пароль:**
Найдите строчку:
`# requirepass foobared`
Уберите решетку (раскомментируйте) и впишите свой надежный пароль. Должно получиться так:
`requirepass ВАШ_СУПЕР_ПАРОЛЬ`

**4. Сохраните файл и перезапустите Redis:**
- В nano нажмите `Ctrl+O` (чтобы сохранить), затем `Enter`.
- Нажмите `Ctrl+X` (чтобы выйти).
- Перезапустите службу:
```bash
sudo systemctl restart redis-server
```

**5. Проверьте статус, чтобы убедиться, что он работает:**
```bash
sudo systemctl status redis-server
```

Всё! Теперь ваш сервер Redis защищен паролем и готов безопасно общаться с торговыми нодами. Не забудьте обновить пароль в файле `.env` на обеих нодах.
